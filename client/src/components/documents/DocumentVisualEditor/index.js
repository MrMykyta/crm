// Thin alias — DocumentVisualEditor has been promoted to the canonical DocumentEngine.
// Kept so any lingering imports keep working; new code should import DocumentEngine.
export { default } from '../DocumentEngine';
